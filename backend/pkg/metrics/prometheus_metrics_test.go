package metrics

import (
	"testing"
)

func TestProduceMetrics(t *testing.T) {

}

func TestServer_RecordDemoMetrics(t *testing.T) {
	/*
		type fields struct {
			Server          *healthz.Server
			refreshInterval int
			dbHandler       *database.Handler
		}
		tests := []struct {
			name   string
			fields fields
		}{
			// TODO: Add test cases.
		}
		for _, tt := range tests {
			t.Run(tt.name, func(t *testing.T) {
				s := &Server{
					Server:          tt.fields.Server,
					refreshInterval: tt.fields.refreshInterval,
					dbHandler:       tt.fields.dbHandler,
				}
				s.StartRecordingMetrics()
			})
		}
	*/
}
